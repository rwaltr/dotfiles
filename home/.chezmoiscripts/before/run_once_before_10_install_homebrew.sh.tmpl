#!/usr/bin/env bash
# vi: ft=sh
# Homebrew installation is managed manually via `rwaltrctl brew`.
#
# We intentionally do NOT install Homebrew during `chezmoi apply`.

set -euo pipefail

echo "Skipping Homebrew install in chezmoi apply."
echo "Use: rwaltrctl brew install-homebrew"
